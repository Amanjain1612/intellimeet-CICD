pipeline {

agent any
triggers { cron('*/5 * * * *') }

    stages {

        stage ('User Confirmation') {
        	steps	{
			input message: 'Proceed or Abort', submitter: 'Proceed,Abort', submitterParameter: 'id'
			} }


			}
			}
			

